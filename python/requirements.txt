# Runtime dependencies (none - uses only Python standard library)
# - os, shutil, threading, tkinter, fnmatch are all built-in

# Build dependencies (for creating .exe)
pyinstaller>=6.0.0
auto-py-to-exe